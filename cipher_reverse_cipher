# Reverse Cipher
# http://inventwithpython.com/hacking (BSD Licensed)

message = str(input("Enter any string to get its reverse\n"))
translated = ''

i = len(message) - 1
while i >= 0:
    translated = translated + message[i]
    i = i - 1

print(translated)
